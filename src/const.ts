export enum AppRoute {
  Root = '/',
  Item = '/item',
  Basket = '/basket',
  NotFound = '*'
}

export enum APIRoute {
  Cameras = '/cameras',
  PromoOffer = '/promo',
  Coupons = '/coupons',
  Reviews = '/reviews',
  Orders = '/orders',
}
